/* import { ref, uploadBytes, getDownloadURL } from 'https://www.gstatic.com/firebasejs/9.6.8/firebase-storage.js';
import { storage } from '../../lib/firestore.js';

export const uploadImage = async (file) => {
  const storageRef = ref(storage,'fotorecetas' + file.name);
  await uploadBytes(storageRef, file);
};

export const obtenerUrl = async (fileRef) => {
  await getDownloadURL(fileRef); */
};
