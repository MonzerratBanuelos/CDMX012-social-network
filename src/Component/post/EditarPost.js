/* import { GetPost } from './GetPost.js';
export const mostrarBoton = () => {
  console.log('hi');
  const datos = GetPost();
  console.log(datos);
  break
}; */
//   const divPost = document.getElementById('postPublicado');
// divPost.querySelectorAll('.borrarPost');
// divPost.forEach((btn) => {
//   if (uidUsuario === currentUid) {
//     btn.setAttribute('hidden', false);

/*
export const mostrarBoton = (uidUsuario, currentUid) => {
  const divPost = document.getElementById('postPublicado');
  divPost.querySelectorAll('.borrarPost');
  divPost.forEach((btn) => {
    if (uidUsuario === currentUid) {
      btn.setAttribute('hidden', false);
    }
  });
};
 */
